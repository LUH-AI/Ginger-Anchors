name: ginger-achors
channels:
  - nvidia
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - pip>=21.2
  - python>=3.8
  - cudatoolkit=11.3
  - pytorch>=1.10
  - pandas
  - numpy
  - smac # requires swig
  - seaborn
  - pytest
  - pip:
    - -r requirements.txt

